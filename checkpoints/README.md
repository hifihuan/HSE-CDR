# Checkpoints (not included in this repository)

This directory is reserved for model weights and training outputs.

Place pretrained and fine-tuned checkpoints here, for example:

```
checkpoints/
├── pretrained_clip/
└── pretrained_detr/
```

Pretrained checkpoints are available upon academic request; see the project README.
